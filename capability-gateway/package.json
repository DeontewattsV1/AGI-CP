{
  "name": "agi-cp-capability-gateway",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "AGI-CP enforcement gateway: ALLOW iff I ∧ K ∧ S ∧ D ∧ A ∧ T ∧ R ∧ P",
  "scripts": {
    "build": "tsc --noEmit",
    "start": "AGICP_START=1 node --experimental-strip-types src/index.ts",
    "dev": "AGICP_START=1 node --experimental-strip-types src/index.ts",
    "test": "node --experimental-strip-types --test test/security-boundary.node-test.ts"
  },
  "dependencies": {
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
